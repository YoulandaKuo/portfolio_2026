# Project Rules

- Only merge to `main` when the user explicitly says it's good to go.
- Always use reusable building blocks/components (`src/components/blocks`) for case study content. If a case study needs something that doesn't already exist as a component, ask before creating a new one.
- Always put a description of what's done (in commit messages and PR descriptions).
